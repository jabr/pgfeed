name: pgfeed
version: 0.1.0
crystal: ">= 1.16.2"

authors:
  - Justin Bradford <jabradford@gmail.com>
repository: "https://github.com/jabr/pgfeed"
license: MIT

description: |
  Consume PostgreSQL logical replication INSERT stream and send to sink

libraries:
  libpq: ">= 17.5"

targets:
  pgfeed:
    main: src/main.cr
    # @todo: not sure if this is possible yet. use `make build` for now
    # flags: -Dpreview_mt -Dexecution_context
